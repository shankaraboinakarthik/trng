SELECT SUM(SAL)
FROM EMP
WHERE JOB = 'SALESMAN';

SELECT MAX(SAL)
FROM EMP
WHERE JOB = 'ANALYST'

SELECT MAX(COMM)
FROM EMP;

SELECT
COUNT(DEPTNO)
FROM EMP;

SELECT SUM(SAL)
FROM EMP;

SELECT MIN(SAL)
FROM EMP;

SELECT AVG(SAL)
FROM EMP
WHERE JOB = 'MANAGER';


